#!/usr/bin/env python3.6
from users import User

    def create_users(uname,password):
        new_users = User(uname,password)
        return new_users

    def save_users(User):
        User.save_users()
